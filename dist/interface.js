"use strict";
const rollNumber = [1, 2, 3, 4, 5];
const roll = [1, 2, 3, 4];
const user = {
    name: ' type alias',
    roll: 'extended',
    age: 20
};
const eUser = {
    name: 'interface',
    roll: 'extended',
    age: 20
};
const addNum = (num1, num2) => num1 + num2;
const multiplyNum = (x, y) => x * y;
